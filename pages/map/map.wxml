<!--pages/map/map.wxml-->
<view>
  <view class='h2'>map</view>
  <map id="map" 
  style="width: 100%; height: 300px;" 
  longitude="{{longitude}}" 
  latitude="{{latitude}}" 
  scale="{{scale}}" 
  markers="{{markers}}"
  show-location
  ></map>
</view>
